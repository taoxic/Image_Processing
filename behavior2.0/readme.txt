人物行为识别使用了BP神经网络代替了lib_svm进行模式识别，两类行为分类
可以达到70%~83%的识别率，数据量太小以致结果不太理想，并且使用了有动量因子的最速下降法，但每次不一定可以找到最小值导致分类失败

第一次采用的参数和配置：
输入层为300个节点
隐含层为25个节点
输出层为2个节点
eb = 0.01;% 误差容限 
eta = 0.6;% 学习率 
mc = 0.8; % 动量因子 
maxiter = 2000;% 最大迭代次数 

程序说明：
function [data,label]=getdata %读入图片sift提取特征，BOW转为特征向量 
function [traind,trainl,testd,testl]=divide(data,label) %分别30个的训练数据和测试数据